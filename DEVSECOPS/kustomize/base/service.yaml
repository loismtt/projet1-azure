apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx-test
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80          # Port interne du cluster
      targetPort: 80    # Port du pod
      nodePort: 30081   # Port exposé sur le master
  type: NodePort
